// es translations of the function catalog. Only texts live here;
// category, arity, parameter types and examples come from ./en.ts, and
// functions missing from this list fall back to English (see ./merge.ts).
import type { FunctionListTranslation } from "./types";

const functionlist: FunctionListTranslation[] = [];

export default functionlist;
